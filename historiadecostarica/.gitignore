# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
# build del export estático (scripts/publish-historiadecostarica.sh)
/.next-export/

# production
/build
/dist/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
.pnpm-debug.log*

# env files
.env*

# typescript
*.tsbuildinfo

# NOTA: `_comercial/` NO se ignora en git — se conserva como memoria del
# proyecto (§3.1). Sí queda fuera del despliegue: Next solo publica `public/`,
# así que la carpeta nunca llega al sitio servido.
