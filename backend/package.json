{
  "name": "elysium-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Transactional billing, meetings and account email service for Elysium CRM",
  "main": "platform-server.js",
  "scripts": {
    "start": "node platform-server.js",
    "check": "node --check platform-server.js && node --check platform-server.test.js",
    "test": "node --test platform-server.test.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "express": "^5.2.1",
    "firebase-admin": "^14.1.0",
    "nodemailer": "^9.0.5"
  }
}
